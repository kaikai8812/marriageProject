# ローカルプレビュー

ローカル開発サーバーを起動してLPをプレビューする。

## 手順

1. 以下のコマンドでローカルサーバーを起動する:
   ```bash
   npx serve src
   ```
2. ブラウザで http://localhost:3000 を開くよう案内する
3. サーバーはバックグラウンドで実行し、ユーザーが作業を続けられるようにする

## 注意
- ポート3000が使用中の場合は別のポートが自動的に割り当てられる
- 停止するには Ctrl+C
